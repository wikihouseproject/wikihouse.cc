<%= f.input :name, autofocus: true %>
<%= f.input :organisation %>

Service(s) you’re interested in providing:*

<% services = [
  'Architectural design',
  'CNC fabrication',
  'Building / contracting services',
  'Project management',
  'Delivery of parts', 'Engineering', 'Insurance', 'Lending', 'Products', 'Other'] %>

<% services.each do |service| %>
<% check_box_tag :services, service.parameterize %>
<% end %>

Message received, thankyou. One of the team will get in contact with you as soon as possible.
